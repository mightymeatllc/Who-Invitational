{
  "name": "who-invitational",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "preview": "node tools/dev-server.mjs",
    "check": "node tools/check.mjs",
    "deploy:staging": "wrangler deploy --env staging",
    "dry": "wrangler deploy --env staging --dry-run",
    "deploy": "wrangler deploy --env=\"\""
  },
  "devDependencies": {
    "playwright": "^1.47.0",
    "wrangler": "^4.124.0"
  }
}
